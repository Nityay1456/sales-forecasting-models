from fbprophet import Prophet\nimport pandas as pd\n\ndef create_forecast(data):\n    model = Prophet()\n    model.fit(data)\n\n    future = model.make_future_dataframe(periods=30)  # Forecast for 30 days\n    forecast = model.predict(future)\n    return forecast\n\nif __name__ == "__main__":\n    # Example usage\n    # Load your dataset here\n    data = pd.read_csv('sales_data.csv')  # Replace with your dataset\n    data['ds'] = pd.to_datetime(data['date_column'])  # Ensure the date column is in datetime format\n    data['y'] = data['sales_column']  # Replace with your sales column\n    forecast = create_forecast(data)\n    print(forecast)